<?xml version="1.0" encoding="utf-8"?>
<resources>
    <string name="app_name">BufferTextInputLayout</string>
    <string name="standard_character_counter_pattern" translatable="false">%1$d / %2$d</string>
    <style name="BufferTextInputLayout" parent="android:Widget">
        <item name="hintTextAppearance">@style/TextAppearance.Design.Hint</item>
        <item name="errorTextAppearance">@style/TextAppearance.Design.Error</item>
        <item name="counterTextAppearance">@style/TextAppearance.Design.Counter</item>
        <item name="counterOverflowTextAppearance">@style/TextAppearance.Design.Counter.Overflow</item>

    </style>
    <declare-styleable name="BufferTextInputLayout">
        <attr format="reference" name="hintTextAppearance"/>
        <!-- The hint to display in the floating label. -->
        <attr name="android:hint"/>
        <!-- Whether the layout's floating label functionality is enabled. -->
        <attr format="boolean" name="hintEnabled"/>
        <!-- Whether the layout is laid out as if an error will be displayed. -->
        <attr format="boolean" name="errorEnabled"/>
        <!-- TextAppearance of any error message displayed. -->
        <attr format="reference" name="errorTextAppearance"/>
        <!-- Whether the layout is laid out as if the character counter will be displayed. -->
        <attr format="boolean" name="counterEnabled"/>

        <attr format="boolean" name="mobileNoEnabled"/>

        <attr format="boolean" name="passwordToggleEnabled"/>

        <attr format="boolean" name="dotToggleEnabled"/>

        <attr format="boolean" name="dropdownToggleEnabled"/>

        <!-- The max length to display in the character counter. -->
        <attr format="integer" name="counterMaxLength"/>
        <!-- TextAppearance of the character counter. -->
        <attr format="reference" name="counterTextAppearance"/>
        <!-- TextAppearance of the character counter when the text is longer than the max. -->
        <attr format="reference" name="counterOverflowTextAppearance"/>
        <attr name="android:textColorHint"/>
        <!-- Whether to animate hint state changes. -->
        <attr format="boolean" name="hintAnimationEnabled"/>
        <!-- The mode to use when formatting the text input layout counter -->
        <attr format="enum" name="counterMode">
            <enum name="descending" value="0"/>
            <enum name="ascending" value="1"/>
            <enum name="standard" value="2"/>
        </attr>
        <!-- The characters remaining value for which the counter should be displayed at -->
        <attr format="integer" name="displayFromCount"/>
    </declare-styleable>
    <declare-styleable name="DesignTheme">
        <attr format="color" name="textColorError"/>
    </declare-styleable>
</resources>